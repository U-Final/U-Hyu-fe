/**
 * 바텀 네비게이션 높이 계산 (고정값)
 */
export const calculateBottomNavHeight = (): number => {
  return 48; // 고정 48px (h-12)
};

/**
 * 바텀 네비게이션 높이 Hook (고정값 반환)
 */
export const useBottomNavHeight = () => {
  return 48; // 고정 48px
};